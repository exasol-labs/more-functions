.. _sys_functions:

``ST_*`` Function Aliases
=========================

For improved compatibility, ``more-functions`` adds alias functions for the ``ST_*`` functions that remove the prefix.

Example:

``AREA`` is an alias for ``ST_AREA``.